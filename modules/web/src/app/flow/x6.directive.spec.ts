import { X6Directive } from './x6.directive';

describe('X6Directive', () => {
  it('should create an instance', () => {
    const directive = new X6Directive();
    expect(directive).toBeTruthy();
  });
});
